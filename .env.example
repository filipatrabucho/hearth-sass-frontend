# Base URL of the HearthGG Laravel API (no trailing slash)
VITE_API_URL=http://localhost:8000

# Discord application (bot) client ID — used to build the "Add bot to server"
# invite link on the Bot settings tab. Same app as DISCORD_CLIENT_ID on the API.
VITE_DISCORD_CLIENT_ID=
